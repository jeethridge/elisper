defmodule Elisper.ApplyActions do
  @moduledoc """
  A nice home for functions that define actions which can be taken by apply.
  """
  def apply_primitive_procedure(procedure), do: nil
  def eval_sequence(procedure_body, procedure)
  def procedure_environment(procedure), do: nil
end
